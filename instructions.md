# Task: Translate this project from Go to Rust

Translate the Go project in `source_go/` into a Rust crate, preserving its
behavior exactly.

## Requirements

- Read the Go source and port it to idiomatic Rust.
- Keep any resource files the code relies on, and use them unchanged.
- Preserve the same public interface and behavior as the original.

## Done when

`cargo test` passes.
